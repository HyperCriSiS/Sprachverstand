import type { Rule } from "../core/rule";

/*
 * Regeln werden erst nach Aufbau eines unabhängigen Testkatalogs ergänzt.
 * Version 0.1.0 enthält bewusst keine migrierten Fremdregeln.
 */
export const defaultRules: readonly Rule[] = [];
