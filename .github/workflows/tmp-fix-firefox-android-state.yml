name: Temporärer Firefox-Android-Zustandsfix

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  fix:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          ref: main
          fetch-depth: 0

      - uses: actions/setup-node@v5
        with:
          node-version: '24'
          cache: npm

      - name: Abhängigkeiten installieren
        run: npm ci

      - name: Firefox-Android-Fixes anwenden
        shell: bash
        run: |
          python <<'PY'
          from pathlib import Path

          def replace_once(path: str, old: str, new: str) -> None:
              p = Path(path)
              text = p.read_text()
              if old not in text:
                  raise SystemExit(f"Muster fehlt in {path}: {old[:80]!r}")
              p.write_text(text.replace(old, new, 1))

          # WebExtension-Typen um flüchtigen Session-Speicher und Tab-URL ergänzen.
          replace_once(
              "src/browser/api.ts",
              'export interface ExtensionTab {\n  readonly id?: number;\n}',
              'export interface ExtensionTab {\n  readonly id?: number;\n  readonly url?: string;\n}'
          )
          replace_once(
              "src/browser/api.ts",
              '    readonly sync: StorageArea;\n    readonly local: StorageArea;',
              '    readonly sync: StorageArea;\n    readonly local: StorageArea;\n    readonly session?: StorageArea;'
          )
          replace_once(
              "src/browser/api.ts",
              '  readonly tabs: {\n    create(createProperties:',
              '  readonly tabs: {\n    get?(tabId: number): Promise<ExtensionTab>;\n    create(createProperties:'
          )

          # Optionsseite explizit als aktiven Tab öffnen. runtime.openOptionsPage()
          # bleibt auf Firefox Android hinter dem Vollbild-Popup sichtbar.
          Path("src/browser/options.ts").write_text('''import type { ExtensionApi } from "./api";

type OptionsPageApi = {
  readonly runtime: Pick<
    ExtensionApi["runtime"],
    "getURL" | "sendMessage"
  >;
  readonly tabs: Pick<ExtensionApi["tabs"], "create" | "query" | "update">;
};

function isValidTabId(tabId: number | undefined): tabId is number {
  return (
    typeof tabId === "number" &&
    Number.isInteger(tabId) &&
    tabId >= 0
  );
}

export function parseOptionsPageTabId(
  urlOrSearch: string | undefined
): number | undefined {
  if (!urlOrSearch) {
    return undefined;
  }

  try {
    const searchParams = urlOrSearch.startsWith("?")
      ? new URLSearchParams(urlOrSearch)
      : new URL(urlOrSearch).searchParams;
    const rawTabId = searchParams.get("tabId");
    if (rawTabId === null || rawTabId.trim() === "") {
      return undefined;
    }

    const tabId = Number(rawTabId);
    return isValidTabId(tabId) ? tabId : undefined;
  } catch {
    return undefined;
  }
}

function optionsPageUrl(api: OptionsPageApi, tabId: number | undefined): string {
  const url = new URL(api.runtime.getURL("options/options.html"));
  if (isValidTabId(tabId)) {
    url.searchParams.set("tabId", String(tabId));
  }
  return url.toString();
}

export async function openOptionsPageInForeground(
  api: OptionsPageApi
): Promise<void> {
  const [activeTab] = await api.tabs.query({
    active: true,
    currentWindow: true
  });
  const inspectedTabId = isValidTabId(activeTab?.id) ? activeTab.id : undefined;

  if (inspectedTabId !== undefined) {
    await api.runtime.sendMessage({
      type: "sprachverstand.set-inspected-tab",
      tabId: inspectedTabId
    });
  }

  // Firefox Android kann runtime.openOptionsPage() hinter dem Vollbild-Popup
  // öffnen. Ein explizit aktiver Tab ist dort deterministischer. Der Popup-
  // Handler schließt anschließend das Vollbild-Popup, damit der neue Tab
  // sichtbar bleibt.
  const createdTab = await api.tabs.create({
    url: optionsPageUrl(api, inspectedTabId),
    active: true
  });

  if (isValidTabId(createdTab.id)) {
    await api.tabs.update(createdTab.id, { active: true });
  }
}
''')

          replace_once(
              "src/popup.ts",
              '  optionsButton.addEventListener("click", () => {\n    void openOptionsPageInForeground(api);\n  });',
              '  optionsButton.addEventListener("click", () => {\n    void (async () => {\n      try {\n        await openOptionsPageInForeground(api);\n        window.close();\n      } catch (error) {\n        console.error("Die erweiterten Einstellungen konnten nicht geöffnet werden.", error);\n      }\n    })();\n  });'
          )

          # Background-Zustand zusätzlich in storage.session spiegeln.
          replace_once(
              "src/background.ts",
              'const api = getExtensionApi();\nconst statesByTab = new Map<number, CachedReplacementState>();\nlet inspectedTabId: number | undefined;',
              'const api = getExtensionApi();\nconst statesByTab = new Map<number, CachedReplacementState>();\nconst runtimeStateStoragePrefix = "sprachverstand.runtime-state.";\nlet inspectedTabId: number | undefined;'
          )

          background = Path("src/background.ts")
          text = background.read_text()
          marker = '  return normalized;\n}\n\nasync function notifyStateUpdate'
          if marker not in text:
              raise SystemExit("Einfügepunkt für Session-Zustand fehlt")
          helper = '''  return normalized;
}

function runtimeStateStorageKey(tabId: number): string {
  return `${runtimeStateStoragePrefix}${tabId}`;
}

function normalizedCachedState(
  value: unknown
): CachedReplacementState | undefined {
  if (!value || typeof value !== "object") {
    return undefined;
  }

  const candidate = value as {
    readonly hostname?: unknown;
    readonly count?: unknown;
    readonly replacements?: unknown;
  };
  if (
    typeof candidate.count !== "number" ||
    !Number.isFinite(candidate.count) ||
    !Array.isArray(candidate.replacements)
  ) {
    return undefined;
  }

  return {
    hostname:
      typeof candidate.hostname === "string" && candidate.hostname
        ? candidate.hostname
        : undefined,
    count: Math.max(0, Math.trunc(candidate.count)),
    replacements: normalizedReplacementEntries(candidate.replacements)
  };
}

async function hostnameFromTab(tabId: number): Promise<string | undefined> {
  if (!api.tabs.get) {
    return undefined;
  }

  try {
    const tab = await api.tabs.get(tabId);
    return hostnameFromSenderUrl(tab.url);
  } catch {
    return undefined;
  }
}

async function readCachedTabState(
  tabId: number
): Promise<CachedReplacementState | undefined> {
  const memoryState = statesByTab.get(tabId);
  if (memoryState) {
    return memoryState;
  }

  const session = api.storage.session;
  if (!session) {
    return undefined;
  }

  try {
    const key = runtimeStateStorageKey(tabId);
    const values = await session.get(key);
    const state = normalizedCachedState(values[key]);
    if (!state) {
      return undefined;
    }

    const restored = {
      ...state,
      hostname: state.hostname ?? (await hostnameFromTab(tabId))
    };
    statesByTab.set(tabId, restored);
    return restored;
  } catch {
    return undefined;
  }
}

async function persistTabState(
  tabId: number,
  state: CachedReplacementState
): Promise<void> {
  const session = api.storage.session;
  if (!session) {
    return;
  }

  try {
    await session.set({ [runtimeStateStorageKey(tabId)]: state });
  } catch {
    // Session-Speicher ist auf älteren Browsern nicht verfügbar.
  }
}

async function removePersistedTabState(tabId: number): Promise<void> {
  const session = api.storage.session;
  if (!session) {
    return;
  }

  try {
    await session.remove(runtimeStateStorageKey(tabId));
  } catch {
    // Der flüchtige Session-Speicher ist nur eine Robustheitsschicht.
  }
}

async function notifyStateUpdate'''
          text = text.replace(marker, helper, 1)
          background.write_text(text)

          replace_once(
              "src/background.ts",
              '  statesByTab.set(tabId, state);\n\n  await Promise.all([',
              '  statesByTab.set(tabId, state);\n  await persistTabState(tabId, state);\n\n  await Promise.all(['
          )
          replace_once(
              "src/background.ts",
              '  statesByTab.delete(tabId);\n\n  try {',
              '  statesByTab.delete(tabId);\n  await removePersistedTabState(tabId);\n\n  try {'
          )
          replace_once(
              "src/background.ts",
              '  const cached = statesByTab.get(tabId);\n  const live = await readLiveReplacementState(tabId);',
              '  const cached = await readCachedTabState(tabId);\n  const live = await readLiveReplacementState(tabId);'
          )
          replace_once(
              "src/background.ts",
              '    statesByTab.set(tabId, merged);\n    return { tabId, text: formatBadgeCount(merged.count) || "0" };',
              '    statesByTab.set(tabId, merged);\n    await persistTabState(tabId, merged);\n    return { tabId, text: formatBadgeCount(merged.count) || "0" };'
          )
          replace_once(
              "src/background.ts",
              '      statesByTab.get(tabId)\n    );',
              '      await readCachedTabState(tabId)\n    );'
          )
          replace_once(
              "src/background.ts",
              '    await updateTabState(tabId, {\n      hostname:\n        statesByTab.get(tabId)?.hostname ?? hostnameFromSenderUrl(sender.url),\n      count: Math.max(0, Math.trunc(message.count)),\n      replacements: statesByTab.get(tabId)?.replacements ?? []\n    });',
              '    const cached = await readCachedTabState(tabId);\n    await updateTabState(tabId, {\n      hostname: cached?.hostname ?? hostnameFromSenderUrl(sender.url),\n      count: Math.max(0, Math.trunc(message.count)),\n      replacements: cached?.replacements ?? []\n    });'
          )
          replace_once(
              "src/background.ts",
              '    const cached = statesByTab.get(message.tabId);\n    const live = await readLiveReplacementState(message.tabId);',
              '    const cached = await readCachedTabState(message.tabId);\n    const live = await readLiveReplacementState(message.tabId);'
          )
          replace_once(
              "src/background.ts",
              '      statesByTab.set(message.tabId, merged);\n      return {',
              '      statesByTab.set(message.tabId, merged);\n      await persistTabState(message.tabId, merged);\n      return {'
          )
          replace_once(
              "src/background.ts",
              '      hostname: undefined,\n      count: Number.parseInt(badgeText, 10) || 0,',
              '      hostname: await hostnameFromTab(message.tabId),\n      count: Number.parseInt(badgeText, 10) || 0,'
          )
          replace_once(
              "src/background.ts",
              'api.tabs.onRemoved.addListener((tabId) => {\n  statesByTab.delete(tabId);\n});',
              'api.tabs.onRemoved.addListener((tabId) => {\n  statesByTab.delete(tabId);\n  void removePersistedTabState(tabId);\n});'
          )

          # Modern-Basisversion an die bereits bestehende Release-Notes-Version angleichen.
          for path in [
              "package.json",
              "package-lock.json",
              "manifests/chromium.json",
              "manifests/firefox.json",
              "manifests/edge.json",
              "manifests/opera.json"
          ]:
              p = Path(path)
              p.write_text(p.read_text().replace('"version": "0.7.0"', '"version": "0.7.2"'))

          Path("tests/browser-options.test.ts").write_text('''import { describe, expect, it, vi } from "vitest";
import {
  openOptionsPageInForeground,
  parseOptionsPageTabId
} from "../src/browser/options";

describe("Optionsseite", () => {
  it("übergibt den Ursprungstab und öffnet die Optionsseite als aktiven Browser-Tab", async () => {
    const calls: string[] = [];
    const sendMessage = vi.fn(async () => {
      calls.push("inspected-tab");
      return undefined;
    });
    const getURL = vi.fn(
      (path: string) => `moz-extension://sprachverstand/${path}`
    );
    const query = vi.fn(async () => [{ id: 17 }]);
    const create = vi.fn(async () => {
      calls.push("create-tab");
      return { id: 31 };
    });
    const update = vi.fn(async () => {
      calls.push("activate-tab");
      return { id: 31 };
    });

    await openOptionsPageInForeground({
      runtime: { sendMessage, getURL },
      tabs: { query, create, update }
    });

    expect(query).toHaveBeenCalledWith({
      active: true,
      currentWindow: true
    });
    expect(sendMessage).toHaveBeenCalledWith({
      type: "sprachverstand.set-inspected-tab",
      tabId: 17
    });
    expect(create).toHaveBeenCalledWith({
      url: "moz-extension://sprachverstand/options/options.html?tabId=17",
      active: true
    });
    expect(update).toHaveBeenCalledWith(31, { active: true });
    expect(calls).toEqual(["inspected-tab", "create-tab", "activate-tab"]);
  });

  it("öffnet ohne gültigen aktiven Ursprungstab trotzdem einen aktiven Options-Tab", async () => {
    const sendMessage = vi.fn(async () => undefined);
    const getURL = vi.fn(
      (path: string) => `moz-extension://sprachverstand/${path}`
    );
    const query = vi.fn(async () => [{}]);
    const create = vi.fn(async () => ({ id: 31 }));
    const update = vi.fn(async () => ({ id: 31 }));

    await openOptionsPageInForeground({
      runtime: { sendMessage, getURL },
      tabs: { query, create, update }
    });

    expect(sendMessage).not.toHaveBeenCalled();
    expect(create).toHaveBeenCalledWith({
      url: "moz-extension://sprachverstand/options/options.html",
      active: true
    });
    expect(update).toHaveBeenCalledWith(31, { active: true });
  });

  it("liest nur gültige Tab-IDs aus Options-URLs und Querystrings", () => {
    expect(
      parseOptionsPageTabId(
        "moz-extension://sprachverstand/options/options.html?tabId=17"
      )
    ).toBe(17);
    expect(parseOptionsPageTabId("?foo=bar&tabId=0")).toBe(0);
    expect(parseOptionsPageTabId(undefined)).toBeUndefined();
    expect(parseOptionsPageTabId("?tabId=-1")).toBeUndefined();
    expect(parseOptionsPageTabId("?tabId=1.5")).toBeUndefined();
    expect(parseOptionsPageTabId("?tabId=abc")).toBeUndefined();
    expect(parseOptionsPageTabId("keine-url")).toBeUndefined();
  });
});
''')

          Path("tests/options-count-context.test.ts").write_text('''import { readFileSync } from "node:fs";
import { describe, expect, it } from "vitest";

const backgroundSource = readFileSync("src/background.ts", "utf8");
const optionsSource = readFileSync("src/options.ts", "utf8");
const browserOptionsSource = readFileSync("src/browser/options.ts", "utf8");
const popupSource = readFileSync("src/popup.ts", "utf8");

describe("Options-Zähler", () => {
  it("übergibt den Ursprungstab und öffnet die Optionsseite sichtbar als aktiven Tab", () => {
    expect(browserOptionsSource).toContain("sprachverstand.set-inspected-tab");
    expect(browserOptionsSource).toContain("api.tabs.create({");
    expect(browserOptionsSource).toContain("active: true");
    expect(browserOptionsSource).toContain('api.runtime.getURL("options/options.html")');
    expect(browserOptionsSource).not.toContain("api.runtime.openOptionsPage()");
    expect(popupSource).toContain("await openOptionsPageInForeground(api)");
    expect(popupSource).toContain("window.close()");
    expect(backgroundSource).toContain("isSetInspectedTabMessage(message)");
    expect(backgroundSource).toContain("inspectedTabId = message.tabId");
    expect(backgroundSource).toContain(
      "inspectedTabId ?? parseOptionsPageTabId(sender.url)"
    );
    expect(backgroundSource).not.toContain("lastCountedTabId");
  });

  it("stellt vollständige Ersetzungsdetails nach einem Background-Neustart aus storage.session wieder her", () => {
    expect(backgroundSource).toContain(
      'const runtimeStateStoragePrefix = "sprachverstand.runtime-state."'
    );
    expect(backgroundSource).toContain("api.storage.session");
    expect(backgroundSource).toContain("readCachedTabState(tabId)");
    expect(backgroundSource).toContain("persistTabState(tabId, state)");
    expect(backgroundSource).toContain("mergeCompatibleState(live, cached)");
  });

  it("bedient das bestehende Options-Protokoll weiterhin kompatibel", () => {
    expect(optionsSource).toContain("sprachverstand.get-inspected-count");
    expect(optionsSource).toContain("sprachverstand.count-updated");
    expect(backgroundSource).toContain("sprachverstand.get-inspected-count");
    expect(backgroundSource).toContain("sprachverstand.count-updated");
  });
});
''')

          Path("tests/background-session-state.test.ts").write_text('''import { afterEach, describe, expect, it, vi } from "vitest";
import type { MessageListener } from "../src/browser/api";

afterEach(() => {
  vi.unstubAllGlobals();
  vi.restoreAllMocks();
  vi.resetModules();
});

describe("Background-Sessionzustand", () => {
  it("stellt Ersetzungsdetails und Hostname nach einem Background-Neustart wieder her", async () => {
    let runtimeListener: MessageListener | undefined;
    const sessionValues: Record<string, unknown> = {
      "sprachverstand.runtime-state.37": {
        count: 14,
        replacements: [
          { original: "Nutzer:innen", replacement: "Nutzer", count: 14 }
        ]
      }
    };

    vi.stubGlobal("browser", {
      runtime: {
        openOptionsPage: vi.fn(),
        getURL: vi.fn((path: string) => `moz-extension://sprachverstand/${path}`),
        sendMessage: vi.fn(async () => undefined),
        onMessage: {
          addListener: vi.fn((listener: MessageListener) => {
            runtimeListener = listener;
          }),
          removeListener: vi.fn()
        }
      },
      action: {
        setBadgeText: vi.fn(async () => undefined),
        getBadgeText: vi.fn(async () => "14"),
        setBadgeBackgroundColor: vi.fn(async () => undefined)
      },
      tabs: {
        get: vi.fn(async () => ({
          id: 37,
          url: "https://www.example.org/artikel"
        })),
        sendMessage: vi.fn(async () => {
          throw new Error("Kein Content-Script-Empfänger");
        }),
        query: vi.fn(async () => [{ id: 37 }]),
        create: vi.fn(async () => ({ id: 1 })),
        update: vi.fn(async () => ({ id: 1 })),
        onUpdated: {
          addListener: vi.fn(),
          removeListener: vi.fn()
        },
        onRemoved: {
          addListener: vi.fn(),
          removeListener: vi.fn()
        }
      },
      i18n: {
        getMessage: vi.fn(() => ""),
        getUILanguage: vi.fn(() => "de")
      },
      storage: {
        local: {},
        sync: {},
        session: {
          get: vi.fn(async (key: string) => ({ [key]: sessionValues[key] })),
          set: vi.fn(async (items: Record<string, unknown>) => {
            Object.assign(sessionValues, items);
          }),
          remove: vi.fn(async (key: string) => {
            delete sessionValues[key];
          })
        },
        onChanged: {
          addListener: vi.fn(),
          removeListener: vi.fn()
        }
      }
    });

    await import("../src/background");
    expect(runtimeListener).toBeDefined();

    const response = (await runtimeListener?.(
      { type: "sprachverstand.get-replacement-state", tabId: 37 },
      {}
    )) as
      | {
          readonly hostname?: string;
          readonly count?: number;
          readonly replacements?: readonly unknown[];
        }
      | undefined;

    expect(response).toMatchObject({
      hostname: "www.example.org",
      count: 14,
      replacements: [
        { original: "Nutzer:innen", replacement: "Nutzer", count: 14 }
      ]
    });
  });
});
''')

          Path("tests/version-consistency.test.ts").write_text('''import { readFileSync } from "node:fs";
import { describe, expect, it } from "vitest";

function readJson(path: string): Record<string, unknown> {
  return JSON.parse(readFileSync(path, "utf8")) as Record<string, unknown>;
}

describe("Versionskonsistenz", () => {
  it("führt die aktuelle Modern-Basisversion in Paket und allen Modern-Manifests", () => {
    const currentVersion = readJson("store/release-notes/current.json").version;
    expect(currentVersion).toBe("0.7.2");

    expect(readJson("package.json").version).toBe(currentVersion);
    expect(readJson("package-lock.json").version).toBe(currentVersion);
    const rootPackage = readJson("package-lock.json").packages as Record<
      string,
      Record<string, unknown>
    >;
    expect(rootPackage[""]?.version).toBe(currentVersion);

    for (const manifest of [
      "manifests/chromium.json",
      "manifests/edge.json",
      "manifests/firefox.json",
      "manifests/opera.json"
    ]) {
      expect(readJson(manifest).version).toBe(currentVersion);
    }
  });
});
''')
          PY

      - name: Vollständige Prüfung
        run: npm run check

      - name: Änderungen committen
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/browser/api.ts src/browser/options.ts src/popup.ts src/background.ts \
            tests/browser-options.test.ts tests/options-count-context.test.ts \
            tests/background-session-state.test.ts tests/version-consistency.test.ts \
            package.json package-lock.json manifests/*.json
          git diff --cached --check
          git commit -m "fix: Firefox-Android-Popupzustand dauerhaft stabilisieren"
          git push origin HEAD:main
